
@{
    ViewBag.Title = "Mvc View dan Controller a veri Gönderme";
}

<h2>Mvc View dan Controller a veri Gönderme</h2>

<h3>Get metoduyla Veri Gönderme</h3>

<div>
    @using (Html.BeginForm("Index", "Mvc09ViewToController", FormMethod.Get))
    {
        @Html.TextBox("txtAra")
        <input type="submit" value="Ara" />
    }
</div>
<p>
    Get metoduyla formdan butona basılırsa veriler adres çubuğu üzerinden get metoduyla QueryString yöntemiyle
    iletilir.Adres çubuğundaki ?txtAra=monitör kısmındaki txtAra ilgili input un name değeridir, monitör ise bu parametrenin taşıdığı
    değer.
</p>
@ViewBag.GetVerisi

<hr />
<h2>Mvc View dan Controller a Post Metodu ile veri gönderme</h2>

@{
    SelectListItem[] liste = new SelectListItem[3]
    {
        new SelectListItem() {Text="Bilgisayar"},
        new SelectListItem() {Text="Monitör"},
        new SelectListItem() {Text="Tablet"}
            };

    <div>
        @using (Html.BeginForm())
        {
            @Html.TextBox("text1")
            <hr />
            @Html.DropDownList("ddListe", liste)
            <hr />
            @Html.CheckBox("cbonay") <label>Onaylıyorum</label>
            <hr />
            <input type="submit" value="Gönder" />

        }
    </div>
    if (IsPost)
    {
        @ViewBag.Mesaj
        <hr />
        @ViewData["Vdata"]
        <hr />
        @TempData["Tdata"]
        <hr />

        <h3>2. yöntem  (Request.Form) ile</h3>
        @ViewBag.Mesaj
        <hr />
        @ViewData["Vdata2"]
        <hr />
        @TempData["Tdata2"]
    }
}